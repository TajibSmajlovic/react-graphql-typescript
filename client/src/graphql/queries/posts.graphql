query Posts($limit: Int!, $cursor: String) {
  getPosts(limit: $limit, cursor: $cursor) {
    hasMore
    items {
      id
      createdAt
      updatedAt
      title
      textSnippet
      creatorId
    }
  }
}
